- alias: "Reset all milights on startup"
  trigger:
    - platform: homeassistant
      event: start
  action:
    - service: light.turn_on
      data:
        rgb_color: [255,255,255] # white
        brightness_pct: 1
        entity_id: group.milights
    - service: light.turn_off
